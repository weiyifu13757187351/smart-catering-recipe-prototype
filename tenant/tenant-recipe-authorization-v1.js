(()=>{
const $=(q,r=document)=>r.querySelector(q),$$=(q,r=document)=>[...r.querySelectorAll(q)];
const merchants=[['杭州市校园餐饮管理中心','SH10001',1],['西湖区学校后勤服务中心','SH10002',1],['南京未来教育服务中心','SH10003',0],['味来小馆·西湖店','SH10004',0]];
const std=[['BZ20260912001',62,9,18,'2026-09-12 14:20','生效中'],['BZ20260905002',58,9,9,'2026-09-05 10:35','生效中'],['BZ20260826003',55,8,5,'2026-08-26 09:10','已撤销']];
const dev=[['SB20260912001','宫保鸡丁-A8标准方案',6,'不可修改','2026-09-12 16:30','生效中'],['SB20260908002','清炒时蔬-S12方案',3,'允许修改','2026-09-08 11:15','已撤销']];
const toast=t=>typeof showToast==='function'&&showToast(t),crumb=t=>{$('#breadcrumb').innerHTML=`菜谱中心 <span>/</span> <b>${t}</b>`};
function hub(){return `<div class="recipe-center-page recipe-hub-page"><div class="page-head"><div><h1>菜谱授权</h1><p>统一管理租户向商户下发的标准菜谱与设备菜谱。</p></div></div><div class="recipe-hub-banner"><div><h2>菜谱授权工作台</h2><p>第一期标准菜谱按全部菜谱持续授权；设备菜谱可集中管理或从设备菜谱库快捷授权。</p></div><span>⊞</span></div><div class="recipe-entry-grid"><button class="recipe-entry-card" data-auth-entry="standard"><i>◉</i><div><b>标准菜谱授权</b><span>授权当前及后续新增的全部已上架标准菜谱</span><div class="recipe-auth-meta"><span><b>2</b>生效批次</span><span><b>27</b>覆盖商户</span></div></div><em>进入管理 →</em></button><button class="recipe-entry-card" data-auth-entry="device"><i>▣</i><div><b>设备菜谱授权</b><span>授权已存档设备菜谱，并配置商户修改权限</span><div class="recipe-auth-meta"><span><b>1</b>生效批次</span><span><b>6</b>覆盖商户</span></div></div><em>进入管理 →</em></button></div></div>`}
function standard(){return `<div class="dish-page"><button class="ghost recipe-back-button recipe-auth-back" data-auth-back>← 返回授权入口</button>${dishPageHead('标准菜谱授权','第一期按全部菜谱持续授权，商户只读使用','<button class="primary" id="newStdAuth">＋ 新建标准菜谱授权</button>')}<div class="auth-notice"><b>全部菜谱持续授权</b><span>同步当前及后续新增的全部已上架标准菜谱与分类；待完善、未上架菜谱不会下发。</span></div><div class="table-card"><table class="auth-batch-table"><thead><tr><th>授权批次</th><th>授权范围</th><th>当前同步菜谱</th><th>分类</th><th>商户</th><th>创建时间</th><th>状态</th><th>操作</th></tr></thead><tbody>${std.map((x,i)=>`<tr><td><b>${x[0]}</b></td><td><span class="auth-kind">全部菜谱</span><span class="sub">持续同步</span></td><td>${x[1]} 个</td><td>${x[2]} 个</td><td>${x[3]} 个</td><td>${x[4]}</td><td><span class="dish-status ${x[5]==='已撤销'?'off':''}">${x[5]}</span></td><td><button class="link-btn" data-detail="${x[0]}">查看</button>${x[5]==='生效中'?`<button class="link-btn" data-std-revoke="${i}">撤销</button>`:''}</td></tr>`).join('')}</tbody></table></div></div>`}
function device(){return `<div class="dish-page"><button class="ghost recipe-back-button recipe-auth-back" data-auth-back>← 返回授权入口</button>${dishPageHead('设备菜谱授权','管理设备菜谱授权、商户修改权限及授权记录','<button class="primary" id="newDevAuth">＋ 新建设备菜谱授权</button>')}<div class="auth-notice"><b>前置条件</b><span>商户必须已获得有效的“全部标准菜谱”授权；设备菜谱必须为已存档。</span></div><div class="table-card"><table class="auth-batch-table"><thead><tr><th>授权批次</th><th>设备菜谱</th><th>商户</th><th>修改权限</th><th>创建时间</th><th>状态</th><th>操作</th></tr></thead><tbody>${dev.map((x,i)=>`<tr><td><b>${x[0]}</b></td><td><b>${x[1]}</b></td><td>${x[2]} 个</td><td><span class="auth-kind device">${x[3]}</span></td><td>${x[4]}</td><td><span class="dish-status ${x[5]==='已撤销'?'off':''}">${x[5]}</span></td><td><button class="link-btn" data-detail="${x[0]}">查看</button>${x[5]==='生效中'?`<button class="link-btn" data-permission="${i}">修改权限</button><button class="link-btn" data-dev-revoke="${i}">撤销</button>`:''}</td></tr>`).join('')}</tbody></table></div></div>`}
function renderAuth(v='hub'){crumb(v==='hub'?'菜谱授权':v==='standard'?'标准菜谱授权':'设备菜谱授权');$('#content').innerHTML=v==='hub'?hub():v==='standard'?standard():device();$$('[data-auth-entry]').forEach(b=>b.onclick=()=>renderAuth(b.dataset.authEntry));$$('[data-auth-back]').forEach(b=>b.onclick=()=>renderAuth());$('#newStdAuth')?.addEventListener('click',stdModal);$('#newDevAuth')?.addEventListener('click',()=>devModal([]));$$('[data-detail]').forEach(b=>b.onclick=()=>toast(`已打开授权批次 ${b.dataset.detail} 详情`));$$('[data-std-revoke]').forEach(b=>b.onclick=()=>{if(confirm('撤销后，该商户的全部标准菜谱及依赖这些菜谱的设备菜谱将无法继续使用。')){std[b.dataset.stdRevoke][5]='已撤销';renderAuth('standard');toast('标准菜谱授权已撤销')}});$$('[data-dev-revoke]').forEach(b=>b.onclick=()=>{if(confirm('确认撤销该设备菜谱授权吗？标准菜谱授权不受影响。')){dev[b.dataset.devRevoke][5]='已撤销';renderAuth('device');toast('设备菜谱授权已撤销')}});$$('[data-permission]').forEach(b=>b.onclick=()=>permissionModal(+b.dataset.permission))}
function baseModal(title,desc){document.body.insertAdjacentHTML('beforeend',`<div class="auth-modal-mask" id="v1AuthModal"><div class="auth-modal"><div class="auth-modal-head"><div><h2>${title}</h2><p>${desc}</p></div><button data-auth-close>×</button></div><div id="v1Steps"></div><div class="auth-modal-body" id="v1Body"></div><div class="auth-modal-foot"><button class="ghost" data-auth-close>取消</button><div class="spacer"></div><button class="ghost" id="v1Prev" hidden>上一步</button><button class="primary" id="v1Next">下一步</button></div></div></div>`);const m=$('#v1AuthModal');$$('[data-auth-close]',m).forEach(b=>b.onclick=()=>m.remove());return m}
const steps=(n,labels)=>{$('#v1Steps').className='auth-steps';$('#v1Steps').innerHTML=labels.map((x,i)=>`<span class="auth-step ${n===i+1?'active':n>i+1?'done':''}"><i>${n>i+1?'✓':i+1}</i>${x}</span>`).join('')};
function merchantHtml(sel,check=false){return `<div class="auth-merchant-layout"><div class="auth-pick-panel"><div class="auth-panel-title"><h3>选择授权商户</h3></div><div class="auth-merchant-list">${merchants.map((x,i)=>`<label class="auth-merchant-row"><input type="checkbox" data-mi="${i}" ${sel.has(i)?'checked':''}><span><b>${x[0]}</b><small>${x[1]}</small>${check?`<small>${x[2]?'已获得全部标准菜谱授权':'未获得全部标准菜谱授权'}</small>`:''}</span></label>`).join('')}</div></div><div class="auth-selected-panel"><div class="auth-panel-title"><h3>已选商户（${sel.size}）</h3></div><div class="auth-selected-list">${[...sel].map(i=>`<div class="auth-selected-item"><b>${merchants[i][0]}</b></div>`).join('')||'暂未选择商户'}</div></div></div>`}
function stdModal(){let n=1,sel=new Set(),m=baseModal('新建标准菜谱授权','第一期授权范围固定为全部菜谱');const draw=()=>{steps(n,['设置授权范围','选择授权商户','授权确认']);$('#v1Prev').hidden=n===1;$('#v1Next').textContent=n===3?'确认授权':'下一步';if(n===1)$('#v1Body').innerHTML=`<div class="auth-scope-grid"><button class="auth-scope-card active"><b>全部菜谱</b><small>当前及后续新增的全部已上架标准菜谱</small></button><button class="auth-scope-card" disabled><em>暂未开放</em><b>自定义菜谱</b><small>按需选择部分标准菜谱</small></button></div><div class="auth-check-note">当前已上架62个菜谱、9个分类，后续新增菜谱上架后自动同步。</div>`;else if(n===2){$('#v1Body').innerHTML=merchantHtml(sel);bindChecks()}else $('#v1Body').innerHTML=`<div class="auth-confirm-metrics"><div><span>授权范围</span><b style="font-size:16px">全部菜谱</b></div><div><span>当前菜谱</span><b>62 个</b></div><div><span>分类</span><b>9 个</b></div><div><span>商户</span><b>${sel.size} 个</b></div></div><div class="auth-check-note">后续新增的已上架标准菜谱及分类将自动同步，商户只读使用。</div>`};const bindChecks=()=>$$('[data-mi]',m).forEach(b=>b.onchange=()=>{b.checked?sel.add(+b.dataset.mi):sel.delete(+b.dataset.mi);draw()});$('#v1Prev').onclick=()=>{n--;draw()};$('#v1Next').onclick=()=>{if(n===2&&!sel.size)return toast('请至少选择一个商户');if(n<3){n++;draw()}else{m.remove();toast('全部标准菜谱授权创建成功')}};draw()}
function selectedRows(){return $$('.device-recipe-check:checked').map(c=>c.closest('tr')).filter(Boolean).map(r=>({name:$('td:nth-child(2) b',r).textContent.trim(),source:$('td:nth-child(3) b',r).textContent.trim(),status:r.dataset.status}))}
function centralDevModal(){const catalog=[{id:'DR202609001',name:'宫保鸡丁-A8标准方案',source:'宫保鸡丁',device:'智能炒菜机器人 A8',status:'已存档'},{id:'DR202609004',name:'紫菜蛋花汤-A8汤品方案',source:'紫菜蛋花汤',device:'智能炒菜机器人 A8',status:'已存档'},{id:'DR202609002',name:'红烧肉设备方案',source:'红烧肉',device:'智能炒菜机器人 A8',status:'开发中'},{id:'DR202609003',name:'清炒时蔬-S12方案',source:'清炒时蔬',device:'万能蒸烤箱 S12',status:'已停用'}],chosen=new Set();let step=1,selected=new Set(),permission='不可修改',modal=baseModal('新建设备菜谱授权','先选择设备菜谱，再选择授权商户并配置修改权限');const picked=()=>catalog.filter(x=>chosen.has(x.id));const draw=()=>{steps(step,['选择设备菜谱','选择商户与权限','授权确认']);$('#v1Prev').hidden=step===1;$('#v1Next').textContent=step===3?'确认授权':'下一步';if(step===1){$('#v1Body').innerHTML=`<div class="device-auth-picker"><div class="device-auth-picker-head"><input id="centralDeviceSearch" placeholder="搜索设备菜谱名称、编号或来源菜谱"><span>已选择 ${chosen.size} 个设备菜谱</span></div>${catalog.map(x=>`<label class="device-auth-picker-row" data-search="${x.id} ${x.name} ${x.source}"><input type="checkbox" data-device-choice="${x.id}" ${chosen.has(x.id)?'checked':''} ${x.status!=='已存档'?'disabled':''}><span><b>${x.name}</b><small>${x.id}</small></span><span>关联：${x.source}</span><span>${x.device}</span><span class="dish-status ${x.status==='已停用'?'off':''}">${x.status}</span></label>`).join('')}</div><div class="auth-check-note">仅“已存档”的设备菜谱可以选择授权。</div>`;$$('[data-device-choice]',modal).forEach(x=>x.onchange=()=>{x.checked?chosen.add(x.dataset.deviceChoice):chosen.delete(x.dataset.deviceChoice);draw()});$('#centralDeviceSearch').oninput=e=>$$('[data-search]',modal).forEach(x=>x.hidden=!x.dataset.search.toLowerCase().includes(e.target.value.toLowerCase()))}else if(step===2){$('#v1Body').innerHTML=merchantHtml(selected,true)+`<div class="auth-permission"><span><b>允许商户二次修改</b><small>修改形成商户版本，不影响租户原始设备菜谱。</small></span><div class="auth-toggle"><button data-p="不可修改" class="${permission==='不可修改'?'active':''}">不允许</button><button data-p="允许修改" class="${permission==='允许修改'?'active':''}">允许</button></div></div>`;$$('[data-mi]',modal).forEach(b=>b.onchange=()=>{b.checked?selected.add(+b.dataset.mi):selected.delete(+b.dataset.mi);draw()});$$('[data-p]',modal).forEach(b=>b.onclick=()=>{permission=b.dataset.p;draw()})}else{const valid=[...selected].filter(i=>merchants[i][2]),invalid=[...selected].filter(i=>!merchants[i][2]);$('#v1Body').innerHTML=`<div class="auth-confirm-metrics"><div><span>设备菜谱</span><b>${chosen.size} 个</b></div><div><span>商户</span><b>${selected.size} 个</b></div><div><span>预计成功</span><b>${valid.length} 个</b></div><div><span>权限</span><b style="font-size:16px">${permission}</b></div></div><div class="auth-confirm-section"><h4>设备菜谱明细</h4>${picked().map(x=>`<div class="auth-confirm-item"><span><b>${x.name}</b><small>　关联：${x.source}</small></span><span>${x.device}</span></div>`).join('')}</div><div class="auth-check-note">${valid.length}个商户满足前置条件。</div>${invalid.length?`<div class="auth-check-note warn">${invalid.map(i=>merchants[i][0]).join('、')}尚未获得全部标准菜谱授权，本次将失败。</div>`:''}`}};$('#v1Prev').onclick=()=>{step--;draw()};$('#v1Next').onclick=()=>{if(step===1&&!chosen.size)return toast('请至少选择一个已存档设备菜谱');if(step===2&&!selected.size)return toast('请至少选择一个商户');if(step<3){step++;draw();return}const valid=[...selected].filter(i=>merchants[i][2]);if(!valid.length)return toast('所选商户均未获得全部标准菜谱授权');modal.remove();toast(valid.length===selected.size?'设备菜谱授权创建成功':`部分成功：${valid.length}个成功，${selected.size-valid.length}个失败`)};draw()}
function devModal(items){let n=1,sel=new Set(),perm='不可修改',list=items.length?items:[{name:'宫保鸡丁-A8标准方案',source:'宫保鸡丁',status:'已存档'}],m=baseModal('新建设备菜谱授权','目标商户须先获得全部标准菜谱授权');const draw=()=>{steps(n,['确认设备菜谱','选择商户与权限','授权确认']);$('#v1Prev').hidden=n===1;$('#v1Next').textContent=n===3?'确认授权':'下一步';if(n===1)$('#v1Body').innerHTML=`<div class="auth-confirm-section"><h4>已选设备菜谱（${list.length}）</h4>${list.map(x=>`<div class="auth-confirm-item"><span><b>${x.name}</b><small>　关联：${x.source}</small></span><span>${x.status}</span></div>`).join('')}</div>`;else if(n===2){$('#v1Body').innerHTML=merchantHtml(sel,true)+`<div class="auth-permission"><span><b>允许商户二次修改</b><small>修改形成商户版本，不影响租户原始设备菜谱。</small></span><div class="auth-toggle"><button data-p="不可修改" class="${perm==='不可修改'?'active':''}">不允许</button><button data-p="允许修改" class="${perm==='允许修改'?'active':''}">允许</button></div></div>`;bind()}else{let ok=[...sel].filter(i=>merchants[i][2]),bad=[...sel].filter(i=>!merchants[i][2]);$('#v1Body').innerHTML=`<div class="auth-confirm-metrics"><div><span>设备菜谱</span><b>${list.length} 个</b></div><div><span>商户</span><b>${sel.size} 个</b></div><div><span>预计成功</span><b>${ok.length} 个</b></div><div><span>权限</span><b style="font-size:16px">${perm}</b></div></div><div class="auth-check-note">${ok.length}个商户满足前置条件。</div>${bad.length?`<div class="auth-check-note warn">${bad.map(i=>merchants[i][0]).join('、')}尚未获得全部标准菜谱授权，本次将失败。</div>`:''}`}};const bind=()=>{$$('[data-mi]',m).forEach(b=>b.onchange=()=>{b.checked?sel.add(+b.dataset.mi):sel.delete(+b.dataset.mi);draw()});$$('[data-p]',m).forEach(b=>b.onclick=()=>{perm=b.dataset.p;draw()})};$('#v1Prev').onclick=()=>{n--;draw()};$('#v1Next').onclick=()=>{if(n===2&&!sel.size)return toast('请至少选择一个商户');if(n<3){n++;draw();return}let ok=[...sel].filter(i=>merchants[i][2]);if(!ok.length)return toast('所选商户均未获得全部标准菜谱授权');m.remove();toast(ok.length===sel.size?'设备菜谱授权创建成功':`部分成功：${ok.length}个成功，${sel.size-ok.length}个失败`)};draw()}
function permissionModal(i){let m=baseModal('修改商户二次修改权限',dev[i][0]),v=dev[i][3];$('#v1Steps').remove();$('#v1Prev').remove();const draw=()=>{$('#v1Body').innerHTML=`<div class="auth-permission"><span><b>允许商户二次修改设备菜谱</b><small>关闭后不删除商户历史版本，但不能继续编辑。</small></span><div class="auth-toggle"><button data-v="不可修改" class="${v==='不可修改'?'active':''}">不允许</button><button data-v="允许修改" class="${v==='允许修改'?'active':''}">允许</button></div></div>`;$$('[data-v]',m).forEach(b=>b.onclick=()=>{v=b.dataset.v;draw()})};$('#v1Next').textContent='保存';$('#v1Next').onclick=()=>{dev[i][3]=v;m.remove();renderAuth('device');toast('商户修改权限已更新')};draw()}
function enhance(){const c=$('#content'),table=$('.device-recipe-table',c);if(!table||$('[data-device-auth-v1]',c))return;const page=$('[data-recipe-extension-page]',c);if(page)page.dataset.deviceAuthV1='';$('.device-list-card',c)?.insertAdjacentHTML('beforebegin','<div class="device-auth-bar"><b>设备菜谱授权</b><span>仅已存档设备菜谱可授权，商户须先获得全部标准菜谱授权。</span><div class="spacer"></div><button class="secondary" id="batchDevAuth" disabled>批量授权</button><button class="ghost" id="viewDevAuth">查看授权记录</button></div>');$$('#deviceRecipeExtensionBody tr',c).forEach(r=>{let cell=$('td:last-child',r),b=document.createElement('button');b.className='link device-auth-link';b.textContent='授权';b.disabled=r.dataset.status!=='已存档';b.title=b.disabled?'仅已存档设备菜谱可授权':'';b.onclick=()=>devModal([{name:$('td:nth-child(2) b',r).textContent.trim(),source:$('td:nth-child(3) b',r).textContent.trim(),status:r.dataset.status}]);cell.appendChild(b)});$('#batchDevAuth').onclick=()=>devModal(selectedRows().filter(x=>x.status==='已存档'));$('#viewDevAuth').onclick=()=>{render('dishAuthorization');setTimeout(()=>renderAuth('device'))};updateBatch()}
function updateBatch(){let b=$('#batchDevAuth');if(!b)return;let n=selectedRows().filter(x=>x.status==='已存档').length;b.disabled=!n;b.textContent=n?`批量授权（${n}）`:'批量授权'}
function formatDeviceBatchCells(){if($('#content h1')?.textContent.trim()!=='设备菜谱授权')return;const recipeNames=[['宫保鸡丁-A8标准方案','紫菜蛋花汤-A8汤品方案'],['清炒时蔬-S12方案']];$$('.auth-batch-table tbody tr').forEach((row,i)=>{const names=recipeNames[i]||[dev[i]?.[1]||'设备菜谱'],cell=row.cells[1];if(!cell)return;const summary=names.slice(0,2).join('、')+(names.length>2?' 等':'');cell.innerHTML=`<b>${names.length} 个设备菜谱</b><span class="sub" title="${names.join('、')}">${summary}</span>`})}
function closeAuthDetail(){$('#recipeAuthorizationDetail')?.remove()}
function detailDrawer(title,subtitle,body){closeAuthDetail();document.body.insertAdjacentHTML('beforeend',`<div class="recipe-modal-mask" id="recipeAuthorizationDetail"><aside class="recipe-modal device-detail-drawer"><div class="recipe-modal-head"><div><h2>${title}</h2><p>${subtitle}</p></div><button data-close-auth-detail>×</button></div><div class="recipe-modal-body device-detail-content">${body}</div><div class="recipe-modal-foot"><div class="spacer"></div><button class="primary" data-close-auth-detail>关闭</button></div></aside></div>`);$$('[data-close-auth-detail]').forEach(x=>x.onclick=closeAuthDetail)}
function showStandardDetail(no){const x=std.find(y=>y[0]===no);if(!x)return;const revoked=x[5]==='已撤销';detailDrawer('标准菜谱授权详情',no,`<div class="readonly-tip">本批次采用“全部菜谱”持续授权，商户只读使用，无需引用或分类映射。</div><section class="device-detail-section"><h3>批次信息</h3><div class="device-detail-grid"><div class="device-detail-field"><span>授权批次</span><b>${x[0]}</b></div><div class="device-detail-field"><span>当前状态</span><b><span class="dish-status ${revoked?'off':''}">${x[5]}</span></b></div><div class="device-detail-field"><span>授权范围</span><b>全部菜谱</b></div><div class="device-detail-field"><span>同步方式</span><b>持续同步</b></div><div class="device-detail-field"><span>创建时间</span><b>${x[4]}</b></div><div class="device-detail-field"><span>操作人</span><b>陈晓宇</b></div></div></section><section class="device-detail-section"><div class="device-detail-section-head"><h3>授权内容</h3><span>当前数据</span></div><div class="auth-confirm-metrics"><div><span>已上架标准菜谱</span><b>${x[1]} 个</b></div><div><span>菜谱分类</span><b>${x[2]} 个</b></div><div><span>授权商户</span><b>${x[3]} 个</b></div><div><span>后续新增</span><b style="font-size:16px">自动同步</b></div></div><div class="auth-confirm-section"><h4>菜谱摘要</h4><div class="auth-confirm-item"><span><b>宫保鸡丁、红烧肉、紫菜蛋花汤等</b><small>　仅同步已上架、分类完整的标准菜谱</small></span><span>${x[1]} 个</span></div></div></section><section class="device-detail-section"><h3>授权商户</h3><div class="merchant-pills"><span>杭州市校园餐饮管理中心</span><span>西湖区学校后勤服务中心</span><span>等 ${x[3]} 个商户</span></div></section><section class="device-detail-section"><h3>同步规则</h3><div class="readonly-tip">当前及后续新增的已上架标准菜谱和对应分类自动同步；菜谱下架后商户端同步停用，恢复上架后自动恢复。${revoked?'本批次已撤销，商户端标准菜谱及其依赖的设备菜谱已停止使用。':''}</div></section>`)}
function showDeviceDetail(no){const x=dev.find(y=>y[0]===no);if(!x)return;const first=x===dev[0],recipes=first?[['宫保鸡丁-A8标准方案','DR202609001','宫保鸡丁','智能炒菜机器人 A8','V3'],['紫菜蛋花汤-A8汤品方案','DR202609004','紫菜蛋花汤','智能炒菜机器人 A8','V2']]:[['清炒时蔬-S12方案','DR202609003','清炒时蔬','万能蒸烤箱 S12','V4']],revoked=x[5]==='已撤销';detailDrawer('设备菜谱授权详情',no,`<div class="readonly-tip">目标商户已获得“全部标准菜谱”授权；本批次只管理设备菜谱及商户二次修改权限。</div><section class="device-detail-section"><h3>批次信息</h3><div class="device-detail-grid"><div class="device-detail-field"><span>授权批次</span><b>${x[0]}</b></div><div class="device-detail-field"><span>当前状态</span><b><span class="dish-status ${revoked?'off':''}">${x[5]}</span></b></div><div class="device-detail-field"><span>设备菜谱</span><b>${recipes.length} 个</b></div><div class="device-detail-field"><span>授权商户</span><b>${x[2]} 个</b></div><div class="device-detail-field"><span>修改权限</span><b>${x[3]}</b></div><div class="device-detail-field"><span>创建时间</span><b>${x[4]}</b></div><div class="device-detail-field"><span>操作人</span><b>陈晓宇</b></div><div class="device-detail-field"><span>使用范围</span><b>当前商户全部组织</b></div></div></section><section class="device-detail-section"><div class="device-detail-section-head"><h3>设备菜谱明细</h3><span>共 ${recipes.length} 个</span></div><div class="device-detail-table-wrap"><table class="device-detail-table"><thead><tr><th>设备菜谱</th><th>关联标准菜谱</th><th>设备型号</th><th>版本</th></tr></thead><tbody>${recipes.map(r=>`<tr><td><b>${r[0]}</b><span class="sub">${r[1]}</span></td><td>${r[2]}</td><td>${r[3]}</td><td>${r[4]}</td></tr>`).join('')}</tbody></table></div></section><section class="device-detail-section"><h3>授权商户</h3><div class="merchant-pills"><span>杭州市校园餐饮管理中心</span><span>西湖区学校后勤服务中心</span><span>等 ${x[2]} 个商户</span></div></section><section class="device-detail-section"><h3>商户使用规则</h3><div class="readonly-tip">${x[3]==='允许修改'?'商户可以编辑并形成自己的设备菜谱版本，不反向修改租户版本；租户有新版本时由已修改商户主动确认更新。':'商户只能查看和使用，不提供编辑入口。未发生商户修改时，租户新版本自动同步。'}${revoked?' 本批次已撤销，商户端保留历史详情但不可继续使用。':''}</div></section>`)}
authorizationPage=hub;const oldBind=bindDishCenter;bindDishCenter=p=>p==='dishAuthorization'?setTimeout(()=>renderAuth()):oldBind(p);document.addEventListener('change',e=>{if(e.target.matches('.device-recipe-check,#checkAllDeviceRecipes'))setTimeout(updateBatch)});new MutationObserver(enhance).observe($('#content'),{childList:true});window.openTenantDeviceAuthorization=devModal;
function enhanceCentralPicker(){const body=$('#v1Body');if(!body)return;$$('[data-device-choice]:disabled',body).forEach(x=>x.closest('.device-auth-picker-row')?.remove());const picker=$('.device-auth-picker',body),head=$('.device-auth-picker-head',body);if(!picker||!head||$('#centralDeviceSelectAll',body))return;head.insertAdjacentHTML('afterend','<label class="auth-select-all"><input type="checkbox" id="centralDeviceSelectAll"><b>全选当前结果</b><small>仅选择当前筛选结果中的已存档设备菜谱</small></label>');const all=$('#centralDeviceSelectAll',body),visible=()=>$$('[data-device-choice]',body).filter(x=>!x.closest('.device-auth-picker-row').hidden);all.checked=visible().length>0&&visible().every(x=>x.checked);all.indeterminate=visible().some(x=>x.checked)&&!all.checked;all.onchange=()=>{const target=all.checked;const next=()=>{const item=visible().find(x=>x.checked!==target);if(item){item.click();setTimeout(next)}else enhanceCentralPicker()};next()};$('#centralDeviceSearch',body)?.addEventListener('input',()=>setTimeout(enhanceCentralPicker))}
document.addEventListener('click',e=>{if(e.target.id==='newDevAuth'){e.preventDefault();e.stopImmediatePropagation();centralDevModal();enhanceCentralPicker();const body=$('#v1Body');new MutationObserver(enhanceCentralPicker).observe(body,{childList:true})}},true);
document.addEventListener('click',e=>{const button=e.target.closest('[data-detail]');if(!button)return;e.preventDefault();e.stopImmediatePropagation();$('#content h1')?.textContent.trim()==='标准菜谱授权'?showStandardDetail(button.dataset.detail):showDeviceDetail(button.dataset.detail)},true);
new MutationObserver(()=>{$$('#deviceRecipeExtensionBody tr').forEach(r=>{const c=$('.device-recipe-check',r);if(c&&r.dataset.status!=='已存档'){c.disabled=true;c.checked=false;c.title='仅已存档设备菜谱可选择授权'}});updateBatch()}).observe($('#content'),{childList:true});
new MutationObserver(formatDeviceBatchCells).observe($('#content'),{childList:true});
let deviceBulkMode='';
function clearDeviceBulkSelection(){$$('.device-recipe-check:checked').forEach(x=>x.click());const all=$('#checkAllDeviceRecipes');if(all){all.checked=false;all.indeterminate=false}}
function applyDeviceBulkMode(){const table=$('.device-recipe-table'),bar=$('.device-auth-bar');if(!table||!bar)return;const checks=$$('.device-recipe-check');checks.forEach(x=>{const archived=x.closest('tr')?.dataset.status==='已存档';x.disabled=!deviceBulkMode||(deviceBulkMode==='auth'&&!archived);if(x.disabled&&x.checked)x.click()});const all=$('#checkAllDeviceRecipes');if(all){all.disabled=!deviceBulkMode;all.onchange=()=>{const target=all.checked,available=checks.filter(x=>!x.disabled);available.forEach(x=>{if(x.checked!==target)x.click()});refreshDeviceBulkMode()}}if(!('modeReady' in bar.dataset)){bar.dataset.modeReady='';$('#batchRecipeTags').style.display='none';bar.innerHTML='<div class="device-bulk-mode-bar" style="width:100%"><b>批量操作</b><span>选择操作后再勾选设备菜谱，两个操作的选择状态相互独立。</span><div class="spacer"></div><button class="secondary" id="startTagBulk">批量设置菜谱标签</button><button class="secondary" id="startAuthBulk">批量授权</button></div>'}bindDeviceBulkMode();refreshDeviceBulkMode()}
function bindDeviceBulkMode(){const tag=$('#startTagBulk'),auth=$('#startAuthBulk'),cancel=$('#cancelDeviceBulk'),next=$('#confirmDeviceBulk');if(tag)tag.onclick=()=>enterDeviceBulkMode('tag');if(auth)auth.onclick=()=>enterDeviceBulkMode('auth');if(cancel)cancel.onclick=exitDeviceBulkMode;if(next)next.onclick=()=>{const rows=selectedRows();if(!rows.length)return toast('请至少选择一个设备菜谱');if(deviceBulkMode==='auth'){devModal(rows);exitDeviceBulkMode()}else{const original=$('#batchRecipeTags');original?.click();exitDeviceBulkMode()}}}
function enterDeviceBulkMode(mode){clearDeviceBulkSelection();deviceBulkMode=mode;const bar=$('.device-auth-bar');bar.dataset.modeReady='';bar.innerHTML=`<div class="device-bulk-mode-bar" style="width:100%"><b class="mode-name">${mode==='tag'?'批量设置菜谱标签':'批量授权'}模式</b><span>${mode==='tag'?'所有状态的设备菜谱均可选择。':'仅“已存档”的设备菜谱可选择。'}</span><div class="spacer"></div><span id="deviceBulkSelected">已选择 0 个</span><button class="ghost" id="cancelDeviceBulk">取消</button><button class="primary" id="confirmDeviceBulk" disabled>${mode==='tag'?'下一步：设置标签':'下一步：授权商户'}</button></div>`;applyDeviceBulkMode()}
function exitDeviceBulkMode(){clearDeviceBulkSelection();deviceBulkMode='';const bar=$('.device-auth-bar');if(bar){delete bar.dataset.modeReady;applyDeviceBulkMode()}}
function refreshDeviceBulkMode(){const selected=$$('.device-recipe-check:checked').length,label=$('#deviceBulkSelected'),next=$('#confirmDeviceBulk'),all=$('#checkAllDeviceRecipes'),available=$$('.device-recipe-check:not(:disabled)');if(label)label.textContent=`已选择 ${selected} 个`;if(next)next.disabled=!selected;if(all&&deviceBulkMode){all.checked=available.length>0&&available.every(x=>x.checked);all.indeterminate=selected>0&&!all.checked}}
document.addEventListener('change',e=>{if(e.target.matches('.device-recipe-check'))setTimeout(refreshDeviceBulkMode)},true);
new MutationObserver(applyDeviceBulkMode).observe($('#content'),{childList:true});
})();
