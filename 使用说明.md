# 原型线上发布 · 使用说明

一句话：**以后改完原型，只要双击 `一键发布.bat`，等 30 秒，线上就是最新版，网址永远不变。**
不用再打包、不用再发文件给别人。

---

## 1. 线上地址（固定，直接发这一个链接）

| 入口 | 网址 |
| --- | --- |
| 总入口（推荐发给别人） | https://weiyifu13757187351.github.io/smart-catering-recipe-prototype/ |
| 租户端 | https://weiyifu13757187351.github.io/smart-catering-recipe-prototype/tenant/index.html |
| 商户端 | https://weiyifu13757187351.github.io/smart-catering-recipe-prototype/merchant/index.html |

入口页会显示**当前线上版本时间 + 提交号**，对方打开一看就知道是不是最新版。

## 2. 日常发布流程（唯一需要记的步骤）

1. 在原来的原型目录里改原型（和以前一样，改完不用打包）：
   - 租户端：`..\restaurant-saas-tenant-prototype-unified-recipe-annotated\`
   - 商户端：`..\merchant-recipe-annotated\`
2. 双击本目录的 **`一键发布.bat`**。
3. 看到「线上部署完成，已是最新版本」即可；别人刷新页面（必要时 `Ctrl + F5`）就能看到新内容。

脚本自动完成：同步原型 → 生成版本号 → 提交 → 推送 → 等待 GitHub Actions 部署完成。

### 常用参数（可选）
在 PowerShell 里执行时可以加参数：

```powershell
# 自定义本次提交说明
.\publish.ps1 -Message "更新商户端设备菜谱"

# 强制以源目录覆盖发布仓库副本（用于跳过安全提醒）
.\publish.ps1 -Force
```

## 3. 目录结构

```text
原型线上发布\                 ← 本发布仓库（= GitHub 仓库）
├─ index.html                 入口页（双入口导航 + 版本信息）
├─ version.json               每次发布自动生成的版本信息
├─ publish.ps1                发布脚本
├─ 一键发布.bat               双击这个
├─ .nojekyll                  必需的（禁用 Jekyll，否则下划线开头的文件不会发布）
├─ .github\workflows\          自动部署工作流（不用动）
├─ tenant\                     租户端原型（自动生成，勿手改）
└─ merchant\                   商户端原型（自动生成，勿手改）
```

## 4. 重要注意事项

- **`tenant\`、`merchant\` 是发布产物，不要直接改这里**：每次发布会按源目录**整体覆盖**（源目录里删掉的文件，线上也会跟着删）。
- 脚本启动时会检查这两个目录有没有被直接改动；如果有，会停下来提醒（确认要覆盖时用 `-Force`）。
- 本仓库是 **Public（公开）** 的：网址不会被搜索引擎主动收录，但**任何拿到链接的人都能访问**，请勿放入不能外泄的内容。若后续需要加访问密码，可以换用 Cloudflare Pages / Netlify 的密码保护，或改用带权限的服务器。
- 首次访问/更新后浏览器可能缓存，看到旧内容按 `Ctrl + F5` 强制刷新即可（原型里的 JS/CSS 已带版本参数）。

## 5. 常见问题

| 现象 | 处理办法 |
| --- | --- |
| 双击 bat 一闪而过 | 在目录里按住 Shift 右键 →「在此处打开 PowerShell 窗口」→ 执行 `.\publish.ps1` 看报错 |
| 提示「推送失败」 | 执行 `gh auth status` 确认 GitHub 登录；或检查网络后重跑 |
| 提示「站点内容没有变化」 | 说明源目录内容与线上一致，属正常，无需发布 |
| 线上没更新 | 打开仓库 → Actions 标签查看部署结果；或执行 `gh run list` |
| 想换线上域名/仓库名 | 改名后仓库地址会变，需同步修改 `publish.ps1` 里的 `$SiteUrl` |
| 原型源目录搬家了 | 修改 `publish.ps1` 顶部的 `$Sources` 路径 |

## 6. 环境依赖（本机已具备）

- Git for Windows（已装，2.55）—— 提交与推送
- GitHub CLI `gh`（已装并已登录账号 `weiyifu13757187351`）—— 查看部署状态；未登录时请执行 `gh auth login`
- 仓库：https://github.com/weiyifu13757187351/smart-catering-recipe-prototype
